---
server:
  port : 5000

swagger:
  apiinfo:
    title: ${spring.application.name}
    description: "@project.description@"
    version: "@project.version@"
    basePackage: "@project.groupId@"


info:
  app:
    name: ${spring.application.name}
